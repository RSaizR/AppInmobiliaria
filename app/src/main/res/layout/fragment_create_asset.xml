<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#DAE7FB">

    <!-- Título: Creación de activo -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Creación de activo"
        android:textSize="28sp"
        android:textStyle="bold"
        android:padding="16dp" />

    <Spinner
        android:id="@+id/spinner_asset_type"
        android:layout_width="match_parent"
        android:layout_height="43dp"
        android:entries="@array/asset_types" />

    <EditText
        android:id="@+id/editTextAddress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ubicación" />

    <EditText
        android:id="@+id/editTextPrice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Precio" />

    <EditText
        android:id="@+id/editTextSquareMeters"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Metros cuadrados" />

    <Button
        android:id="@+id/buttonSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#82BF7A"
        android:text="Guardar" />

</LinearLayout>
